define(["jade"],function(jade){

return function template(locals) {
var buf = [];
var jade_mixins = {};
var jade_interp;

buf.push("<p>hello this is dashboard jade</p>");;return buf.join("");
};

});
